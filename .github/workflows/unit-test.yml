name: unit-test

on: 
  workflow_call:

jobs:
  test: 
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
        name: Checkout

      - name: run tests
        working-directory:  ./
        run: |
          dotnet test \ 
            --configuration Release \
            --filter "TestProject1.csproj"
