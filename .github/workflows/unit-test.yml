name: unit-test

on: 
  workflow_call:

jobs:
  test: 
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout

      - name: run tests
        working-directory:  ./
        run: |
          dotnet test \ 
            --configuration Release \
            --filter "FullyQualifiedName~TestProject"
